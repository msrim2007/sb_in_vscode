server:
    port: 8080
    servlet:
        context-path: /

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/sb_of_vsc?serverTimezone=Asia/Seoul
        username: root
        password: root1234
    thymeleaf:
        prefix: classpath:templates/
        suffix: .html
        cache: false